global:
  debug: true
  devMode:
    enabled: true
    baseDir: '/set/to/$(pwd)'
  ingress:
    host: localhost
ingress-nginx:
  enabled: false
  controller:
    admissionWebhooks:
      enabled: false
sheba:
  input:
    internal:
      enabled: true
mongodb:
  service:
    type: NodePort
    nodePorts:
      mongodb: 32717