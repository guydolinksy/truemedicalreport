global:
  debug: true
  devMode:
    enabled: true
    baseDir: '/set/to/$(pwd)'
  ingress:
    host: localhost
ingress-nginx:
  enabled: false
  controller:
    admissionWebhooks:
      enabled: false
sheba:
  input:
    type: internal
mongodb:
  service:
    type: NodePort
    nodePorts:
      mongodb: 32717